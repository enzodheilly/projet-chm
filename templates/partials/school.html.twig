<section id="ecole" class="section bg-background">
    <div class="container">
        <div class="section-content">
            <!-- Section Header -->
            <div class="section-header">
                <h2 class="section-title">
                    École d'<span class="text-primary">Haltérophilie</span>
                </h2>
                <p class="section-description">
                    Découvrez notre école d'haltérophilie reconnue pour la qualité de son enseignement 
                    et l'excellence de ses résultats en compétition.
                </p>
            </div>

            <div class="school-grid">
                <!-- Image -->
                <div class="school-image order-2 lg-order-1">
                    <div class="image-container">
                        <img src="{{ asset('images/school-training.jpg') }}" alt="École d'haltérophilie" class="school-img">
                        <div class="image-overlay"></div>
                    </div>
                </div>

                <!-- Content -->
                <div class="school-content order-1 lg-order-2">
                    <div class="school-text">
                        <h3 class="school-subtitle">Formation d'Excellence</h3>
                        <p class="text-muted">
                            Notre école d'haltérophilie forme les champions de demain avec une pédagogie 
                            adaptée à chaque âge et chaque niveau. De l'initiation au haut niveau, 
                            nos entraîneurs diplômés accompagnent chaque athlète dans sa progression.
                        </p>
                        <p class="text-muted">
                            Nous mettons l'accent sur l'apprentissage technique, le développement physique 
                            harmonieux et l'esprit sportif dans un environnement sécurisé et motivant.
                        </p>
                    </div>

                    <a href="#infos" class="btn btn-hero btn-lg">Inscrire mon enfant</a>
                </div>
            </div>

            <!-- Programs -->
            <div class="programs-grid">
                {% for program in programs %}
                    <div class="program-card">
                        <div class="program-header">
                            <div class="program-icon">
                                <i data-lucide="{{ program.icon }}" class="icon"></i>
                            </div>
                            <h4 class="program-title">{{ program.title }}</h4>
                            <p class="program-age">{{ program.age }}</p>
                        </div>
                        <div class="program-content">
                            <p class="program-description">{{ program.description }}</p>
                            <ul class="program-features">
                                {% for feature in program.features %}
                                    <li class="program-feature">
                                        <div class="feature-bullet"></div>
                                        {{ feature }}
                                    </li>
                                {% endfor %}
                            </ul>
                        </div>
                    </div>
                {% endfor %}
            </div>

            <!-- Schedules -->
            <div class="schedule-card">
                <div class="schedule-header">
                    <div class="schedule-icon">
                        <i data-lucide="clock" class="icon"></i>
                    </div>
                    <h3 class="schedule-title">Horaires d'Entraînement</h3>
                </div>
                <div class="schedule-content">
                    <div class="schedule-grid">
                        {% for schedule in schedules %}
                            <div class="schedule-day">
                                <h4 class="day-name">{{ schedule.day }}</h4>
                                {% for session in schedule.sessions %}
                                    <div class="session-info">
                                        <p class="session-time">{{ session.time }}</p>
                                        <p class="session-type">{{ session.type }}</p>
                                        <p class="session-level">{{ session.level }}</p>
                                    </div>
                                {% endfor %}
                            </div>
                        {% endfor %}
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>