{% block title %}{% endblock %}

{% block body %}

	<style>
		/* ===============================
   🎨 Page de désinscription CHM
   =============================== */
		.newsletter-unsubscribe-success {
			min-height: 80vh;
			display: flex;
			align-items: center;
			justify-content: center;
			background: radial-gradient(circle at top, #121212, #0a0a0a);
			color: #eee;
			padding: 60px 20px;
			font-family: 'Inter', sans-serif;
		}

		.unsubscribe-card {
			background: #161616;
			border: 1px solid rgba(255, 255, 255, 0.05);
			border-radius: 16px;
			box-shadow: 0 0 25px rgba(0, 0, 0, 0.5);
			padding: 40px 50px;
			max-width: 600px;
			text-align: center;
			animation: fadeInUp 0.6s ease forwards;
		}

		.unsubscribe-card h1 {
			color: #e53935;
			font-size: 2rem;
			margin-bottom: 20px;
			font-weight: 700;
			letter-spacing: 0.5px;
		}

		.unsubscribe-card p {
			color: #ccc;
			font-size: 1.05rem;
			line-height: 1.6;
			margin-bottom: 30px;
		}

		.unsubscribe-card strong {
			color: #fff;
			font-weight: 600;
		}

		.btn-return {
			display: inline-block;
			background: linear-gradient(90deg, #d32f2f, #b71c1c);
			color: #fff;
			text-decoration: none;
			padding: 12px 24px;
			border-radius: 10px;
			font-weight: 600;
			transition: all 0.25s ease;
			box-shadow: 0 4px 15px rgba(211, 47, 47, 0.3);
		}

		.btn-return:hover {
			background: linear-gradient(90deg, #e53935, #c62828);
			transform: translateY(-2px);
			box-shadow: 0 6px 20px rgba(211, 47, 47, 0.4);
		}

		@keyframes fadeInUp {
			from {
				opacity: 0;
				transform: translateY(20px);
			}
			to {
				opacity: 1;
				transform: translateY(0);
			}
		}
	</style>
	<section class="newsletter-unsubscribe-success" style="text-align:center; padding:100px 20px;">
		<div style="max-width:600px; margin:auto; background:#111; border-radius:12px; padding:40px; box-shadow:0 0 25px rgba(0,0,0,0.3);">
			<h1 style="color:#d32f2f; margin-bottom:20px;">Désinscription réussie</h1>
			<p style="font-size:16px; color:#ddd; line-height:1.6;">
				Votre adresse
				<strong>{{ email }}</strong>
				a bien été retirée de notre liste de diffusion.<br>
				Vous ne recevrez plus de newsletter du CHM Saleux.
			</p>
			<a href="{{ path('home') }}" class="btn" style="display:inline-block; margin-top:25px; background:#d32f2f; color:white; padding:10px 20px; border-radius:8px; text-decoration:none;">
				Retour à l’accueil
			</a>
		</div>
	</section>
{% endblock %}
