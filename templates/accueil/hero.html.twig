{% extends 'base.html.twig' %}

{% block stylesheets %}
	{{ parent() }}
	<link rel="stylesheet" href="{{ asset('css/accueil/hero/hero.css') }}">
	<style>
		/* Section plein écran avec dégradé orange */
		.hero-section {
			display: flex;
			justify-content: space-between;
			align-items: center;
			min-height: 80vh;
			position: relative;
			padding: 0 5%;
			overflow: hidden;
			background: linear-gradient(120deg,#f97316,#ec4899);
		}

		.hero-section::before {
			content: "";
			position: absolute;
			top: 0;
			left: 0;
			width: 100%;
			height: 100%;
			background: rgba(0, 0, 0, 0.4);
			z-index: 1;
		}

		.hero-video {
			position: absolute;
			top: 70px;
			left: 0;
			width: 100%;
			height: 100%;
			object-fit: cover; /* pour remplir toute la section */
			z-index: 0; /* derrière tout le contenu */
		}

		/* Contenu à gauche */
		.hero-content {
			position: relative;
			margin-top: 90px;
			z-index: 2;
			display: flex;
			flex-direction: column;
			justify-content: center;
			max-width: 50%;
			margin-bottom: 0.5rem;
			transform: scale(1.2); /* 👈 agrandit légèrement tout le container */
			transform-origin: left center; /* 👈 garde l'alignement à gauche */

		}

		.hero-content p {
			font-size: 1.2rem;
			font-weight: 300;
		}

		.hero-content h1 {
			font-size: 2.5rem;
			font-weight: 700;
			margin-bottom: 0.5rem;
		}

		.hero-buttons a {
			display: inline-block;
			margin-right: 1rem;
			padding: 0.5rem 1rem;
			border-radius: 0.5rem;
			text-decoration: none;
			font-weight: 500;
			transition: 0.3s;
		}

		/* Statistiques */
		.hero-inner {
			animation: slideIn 1.3s ease-out forwards;
			text-align: left;
		}

		.hero-line {
			font-size: 1.7rem;
			font-weight: 450;
			color: white;
			line-height: 1.2;
			margin: 0.25rem 0; /* resserrer les lignes */
		}

		/* Bouton */
		.btn-en-savoir-plus {
			display: inline-block;
			margin-top: 1rem;
			padding: 0.55rem 0.7rem;
			background: white;
			color: #f97316;
			font-weight: 300;
			border-radius: 0.5rem;
			text-decoration: none;
			transition: all 0.3s ease;
			width: fit-content;
		}

		.btn-en-savoir-plus:hover {
			color: black;
		}

		.nous-rejoindre {
			background: #0d0d0d;
			color: white;
			padding: 0.55rem 0.7rem;
			border-radius: 0.5rem;
			transition: all 0.3s ease;
			text-decoration: none;
			font-weight: 300;
			margin-top: 1rem;
			display: inline-block;
		}

		.hero-buttons .nous-rejoindre:hover {
			background: #f97316;
			color: black;
		}

		.hero-buttons {
			display: flex;
			gap: 0.75rem; /* 👈 Ajoute un espace entre les boutons */
			margin-top: 0.75rem;
		}

		/* FFHM */
		.ffhm-container {
			display: flex;
			align-items: center;
			gap: 0.75rem;
			margin-top: 1rem;
		}

		.ffhm-logo {
			height: 95px;
			width: auto;
		}

		@keyframes slideIn {
			from {
				opacity: 0;
				transform: translateX(800px);
			}
			to {
				opacity: 1;
				transform: translateX(0);
			}
		}

		.thin-text {
			font-weight: 300;
		}

		/* Titres */
		.club-title {
			font-weight: 700;
			font-size: 2rem;
			color: white;
			margin: 0;
		}

		.club-subtitle {
			font-size: 1.2rem;
			font-weight: 300;
			color: white;
			margin: 0;
		}

		/* Image à droite */
		.hero-image {
			max-width: 45%;
			margin-top: 85px;
			margin-right: 35px;
			height: auto;
			object-fit: contain;
			border-radius: 1rem;
			box-shadow: 0 0 20px rgba(0, 0, 0, 0.3);
		}

		/* responsive */
		@media(max-width:768px) {
			.hero-section {
				flex-direction: column;
				text-align: center;
			}
			.hero-content {
				max-width: 100%;
				margin-bottom: 1rem;
			}
			.hero-image {
				max-width: 90%;
				margin-top: 1rem;
			}
		}
	</style>
{% endblock %}

{% block body %}
	<section id="accueil" class="hero-section">

		<video class="hero-video" autoplay muted loop playsinline>
			<source src="{{ asset('videos/video.mp4') }}" type="video/mp4">
			Votre navigateur ne supporte pas les vidéos HTML5.
		</video>

		<!-- Partie gauche : texte -->
		<div
			class="hero-content">
			<!-- Titre et sous-titre -->
			<div class="club-title">CLUB HALTÉROPHILIE & MUSCULATION</div>
			<div class="club-subtitle">Relevez vos limites et dépassez-vous chaque jour !</div>

			<!-- Statistiques -->
			<div class="hero-inner">
				<div class="hero-line" id="adherents-counter">
					+ 0
					<span class="thin-text">ADHÉRENTS</span>
				</div>
				<div class="hero-line">
					+ 30
					<span class="thin-text">COMPÉTITIONS</span>
				</div>
			</div>

			<div class="hero-buttons">
				<a href="#plus" class="nous-rejoindre">Nous rejoindre</a>
				<a href="#plus" class="btn-en-savoir-plus">En savoir plus sur nous</a>
			</div>

			<!-- Logo et texte FFHM -->
			<div class="ffhm-container">
				<img src="{{ asset('images/ffhm.png') }}" alt="Logo FFHM" class="ffhm-logo">
			</div>
		</div>


		<!-- Partie droite : image -->
		<img src="{{ asset('images/hero-gym.jpg') }}" alt="Salle de sport" class="hero-image">
	</section>

	 <script>
																																																																																																																																																																																																																																										document.addEventListener("DOMContentLoaded", function () {
																																																																																																																																																																																																																																										    let counter = document.getElementById("adherents-counter");
																																																																																																																																																																																																																																										    let target = 150;
																																																																																																																																																																																																																																										    let count = 0;
																																																																																																																																																																																																																																										    let speed = 20;
																																																																																																																																																																																																																																										
																																																																																																																																																																																																																																										    let interval = setInterval(() => {
																																																																																																																																																																																																																																										        count++;
																																																																																																																																																																																																																																										        counter.innerHTML = `+ ${count} <span class="thin-text">ADHÉRENTS</span>`;
																																																																																																																																																																																																																																										        if (count >= target) clearInterval(interval);
																																																																																																																																																																																																																																										    }, speed);
																																																																																																																																																																																																																																										});
																																																																																																																																																																																																																																									</script>
{% endblock %}
