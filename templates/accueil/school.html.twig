{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/accueil/ecole/ecole.css') }}">
{% endblock %}

{% block body %}

<section id="ecole" class="section bg-background">
  <div class="container">
    <div class="section-content">
      <!-- Section Header -->
      <div class="section-header">
        <h2 class="section-title">École d'<span class="text-primary">Haltérophilie</span></h2>
        <p class="section-description">Découvrez notre école d'haltérophilie reconnue pour la qualité de son enseignement et l'excellence de ses résultats en compétition.</p>
      </div>

      <div class="school-grid">
        <!-- Image -->
        <div class="school-image order-2 lg-order-1">
          <div class="image-container">
            <img src="{{ asset('images/school-training.jpg') }}" alt="École d'haltérophilie" class="school-img" />
            <div class="image-overlay"></div>
          </div>
        </div>

        <!-- Content -->
        <div class="school-content order-1 lg-order-2">
          <div class="school-text">
            <h3 class="school-subtitle">Formation d'Excellence</h3>
            <p class="text-muted">Notre école d'haltérophilie forme les champions de demain avec une pédagogie adaptée à chaque âge et chaque niveau. De l'initiation au haut niveau, nos entraîneurs diplômés accompagnent chaque athlète dans sa progression.</p>
            <p class="text-muted">Nous mettons l'accent sur l'apprentissage technique, le développement physique harmonieux et l'esprit sportif dans un environnement sécurisé et motivant.</p>
          </div>

          <a href="#infos" class="btn btn-hero btn-lg">Découvrir -></a>
        </div>
      </div>

      <!-- Programs -->
      <div class="programs-grid">
        {% for program in programs %}
          <div class="program-card">
            <div class="program-header">
              <div class="program-icon">
                <i data-lucide="{{ program.icon }}" class="icon"></i>
              </div>
              <h4 class="program-title">{{ program.title }}</h4>
              <p class="program-age">{{ program.age }}</p>
            </div>
            <div class="program-content">
              <p class="program-description">{{ program.description }}</p>
              <ul class="program-features">
                {% for feature in program.features %}
                  <li class="program-feature">
                    <div class="feature-bullet"></div>
                    {{ feature }}
                  </li>
                {% endfor %}
              </ul>
            </div>
          </div>
        {% endfor %}
      </div>
    </div>
  </div>
</section>

{% endblock %}
